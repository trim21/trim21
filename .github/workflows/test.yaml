name: test

on:
  push:
    branches:
      - 'master'

jobs:
  test:
    runs-on: windows-latest
    steps:
      -
        name: Checkout
        uses: actions/checkout@v4

      - run: pkg-config --help
      - run: pkg-config --libs libpq
